# ===============================
# Thư viện cơ bản Flask & hỗ trợ
# ===============================
Flask==2.3.3
Werkzeug==2.3.7
Flask-APScheduler==1.13.1
Flask-SocketIO==5.5.1
Flask-Mail==0.9.1
itsdangerous==2.1.2
python-dotenv==1.1.1
openpyxl==3.1.3
pillow==10.1.0
numpy==1.26.1
mysql-connector-python==8.1.0

# ===============================
# Thư viện AI / face recognition
# ===============================
# ⚠️ face_recognition cần Docker hoặc pre-built wheel vì phụ thuộc dlib + CMake
face_recognition==1.3.0

# ⚠️ google-genai phải cài đúng version
google-genai==0.14.1

# ===============================
# Các thư viện chuẩn Python
# ===============================
# Không cần cài, Python built-in: traceback, base64, os, io, datetime, random, json, smtplib, email
